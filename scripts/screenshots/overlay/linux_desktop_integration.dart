// SUSTITUTO PARA LAS CAPTURAS — no forma parte de la app que se publica.
//
// El original registra el `.desktop` del AppImage con `dart:io`. En el
// navegador no aplica y no compila. Ver scripts/screenshots/README.md.
class LinuxDesktopIntegration {
  static Future<void> registrarSiHaceFalta() async {}
}
